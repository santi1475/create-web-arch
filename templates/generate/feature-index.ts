/**
 * Public API — {{FEATURE_NAME}} feature
 *
 * Only import from this file when consuming this feature from outside.
 * Direct imports into subfolders are considered private/internal.
 *
 * @example
 *   import { {{FEATURE_EXPORT_EXAMPLE}} } from "@features/{{FEATURE_SLUG}}"
 */

// ── Components ────────────────────────────────────────────────────────────────
// export { default as {{FEATURE_NAME}}Page } from "./components/{{FEATURE_NAME}}Page";

// ── Hooks ─────────────────────────────────────────────────────────────────────
// export { use{{FEATURE_NAME}} } from "./hooks/use{{FEATURE_NAME}}";

// ── Services ──────────────────────────────────────────────────────────────────
// export { {{FEATURE_NAME_LOWER}}Service } from "./services/{{FEATURE_NAME_LOWER}}Service";

// ── Types ─────────────────────────────────────────────────────────────────────
// export type { {{FEATURE_NAME}}DTO, {{FEATURE_NAME}}State } from "./types";
